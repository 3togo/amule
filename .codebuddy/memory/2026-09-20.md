# 2026-09-20

## aMule upstream / PR target
- Correct GitHub upstream org for aMule is **https://github.com/amule-org/amule** (NOT `amule-project`). User wants any PR from the `search-feature` branch to target `amule-org/amule`.
- User is on branch `search-feature`. There is an in-progress merge with many unmerged (`UU`) files, including `src/LibSocketAsio.cpp`, `.gitignore`, `src/FileLock.h`, `MD4Hash.h`, many `Search*` files, kademlia `Search*`, `Indexed.cpp`, `KademliaUDPListener.cpp`, `libs/ec/abstracts/CMakeLists.txt`, etc.

## LibSocketAsio.cpp conflict resolution
- The pasted conflict was between `fix/boost-asio-modernization` (legacy boost::bind style) and a modernized side (`std::enable_shared_from_this`, atomics, `EnableTcpKeepalive`, `shared_ptr` wrappers, #384 wake-from-sleep lifetime fix). User chose **modernized side + header patch**.
- The modernized side (as pasted) had internal bugs: `std::make_unique<ip::tcp::socket>` assigned to raw `ip::tcp::socket *`; `delete[]` on a `std::unique_ptr<char[]>` read buffer (UDP); dangling `m_timer` member + obsolete `HandleDestroy`. Fixed during resolution.
- Resolved clean file lives at `/tmp/LibSocketAsio.resolved.cpp` (0 conflict markers, monitor hooks + OnLost() change applied). It must be copied into `src/LibSocketAsio.cpp` and verified, then the header patch (`shared_ptr` members, `LinkSocketImpl(shared_ptr)`, `EnableTcpKeepalive` decl, `<memory>`) kept in sync.
- CORRECTION (resolved): The rebase `unified-search onto upstream/master` is ALREADY COMMITTED (HEAD = `63a283041`). There were no remaining `UU`/conflict-marker files — the conflict resolution was already done by the user. `src/LibSocketAsio.cpp` ended up in the modernized form.
- Audit of the committed modernized code: the three gross bugs (`make_unique<ip::tcp::socket>` misassign, `unique_ptr<char[]>` read buffer + `delete[]`, dangling `m_timer`/`HandleDestroy`) are ABSENT. `LibSocket.h` has all needed decls (`<memory>`, `EnableTcpKeepalive`, `LinkSocketImpl(shared_ptr)`, `shared_ptr` members).
- `OnLost(int)` dispatch is CORRECT: base `CLibSocket::OnLost(int)` (LibSocket.h:149); `ECMuleSocket` (MI: `CECSocket, CLibSocket`) overrides `OnLost(int)` and bridges via `static_cast<CECSocket*>(this)->OnLost()` — a valid upcast + virtual call that reaches `CECServerSocket`/`CRemoteConnect`/`CECSocket` `OnLost()` (no-param) overrides. `CWebSocket` and `EMSocket` override `OnLost(int)` directly. No regression.
- Working tree is clean (only `.codebuddy/memory/2026-09-20.md` modified). PR to amule-org/amule is deferred per user ("not yet — resolve first").

## (b) Static correctness pass — search-feature files (no full build; user canceled heavy build)
Scope: SearchDlg*, SearchList*, SearchListCtrl*, SearchFile*, kademlia Search*/Indexed*/ParallelSearch*/KademliaUDPListener*, plus new src/search/* and SearchStateManager/SearchTimeoutManager/SimpleSearchCache.
Checks run:
- No `<<<<<<<`/`>>>>>>>`/`=======` conflict markers anywhere in tracked tree (prior false positives were CMake bars, CHANGELOG text, XPM art, utp comment).
- Preprocessor #if/#ifdef/#ifndef vs #endif balanced in all rewritten files (no broken blocks).
- GUI files properly wired to new src/search subsystem: SearchDlg.cpp, SearchList.cpp, SearchListCtrl.cpp include & call UnifiedSearchManager / SearchIdGenerator / SearchResultRouter / PerSearchState etc. SearchFile.cpp is pure data model (no subsystem dep needed).
- No duplicate method definitions: all apparent dupes are overloads (e.g. CreateSearchData(CSearchParams&) vs (search::SearchParams&), StopSearch(bool) vs (long,bool), ProcessSearchResponse(CMemFile&) vs (uint8_t*,uint32_t), getProgress(size_t) vs ()).
Limitation: subtle errors (signature mismatch, undefined symbol, dropped merge logic, ODR) need an actual compile. User canceled the full `cmake --build build` run; a real build was NOT completed. Definitive correctness still requires a build — offer to run targeted/full build on request.

## FULL BUILD FAILED — branch does not compile after rebase
Ran `cmake --build build -j` (incremental). 55 errors. Root cause: the search-feature
rebase rewrote `CSearchList`/`CSearchDlg` (removed the old browse-search lifecycle +
helpers) but the rest of the app was NOT updated and still calls the removed API:
- Fatal: `src/SearchListCtrl.h` includes `MuleListCtrl.h` which does NOT exist in tree.
- `CSearchList` missing (callers ExternalConn.cpp x42, BaseClient.cpp, KnownFile.cpp):
  AllocateEd2kId, RegisterBrowseSearch, GetKnownSearchIds, GetSearchFileByID,
  GetBrowsePeerEcid, GetSearchLifecycle(Kind|State|Percent)(ById), GetSearchStringById,
  StopSearchById, StopInFlightEd2kSearch, IsKnownSearchId (x5), GetAllSearchFilesByID,
  GetCurrentSearchResultCount, AddFileToDownloadByEcid, and type `BrowseSearch`.
- `CSearchDlg` missing (callers FriendListCtrl.cpp, GuiEvents.cpp, PrefsUnifiedDlg.cpp):
  ActivateBrowseTabIfOpen, RefreshVisibleTabProgress, SetBrowseStatus, CloseSearchTab,
  OnSearchAdded, EnsureBrowseTab, ApplySearchHistoryPref.
None of those caller files were modified by the branch commit (git show --stat HEAD),
so the rebase was committed WITHOUT ever compiling against the full tree.
The new `src/search/*` subsystem + SearchDlg/SearchList/SearchListCtrl DO compile
(unit tests pass) — only the integration with the rest of the app is broken.
FIX requires design decision (preserve browse via bridges / stub the removed feature /
user supplies pre-rebase caller versions). Asked user.

## (b) Unit tests — DONE and PASSING (3 suites, 11 cases)
- Added `unittests/tests/{SearchIdGeneratorTest,SearchTimeoutManagerTest,SearchParamsTest}.cpp`
  + CMake entries in `unittests/tests/CMakeLists.txt`. All 3 build, run, pass under ctest.
- SearchIdGeneratorTest (5): monotonic/unique/non-zero ids, reserve rejects 0+dups, release, clearAll.
- SearchTimeoutManagerTest (4): register/unregister/type, config getters/setters, elapsed/remaining
  shrink, timeout fires callback+counter. Note: swallows benign wx timer-destructor assert
  (no event loop in harness) via wxSetAssertHandler inside first test; uses 1200ms sleep to be deterministic.
- SearchParamsTest (2): default+validity, equality. (enum-class ASSERT_EQUALS needs ASSERT_TRUE.)
These validate the NEW subsystem in isolation only; they do NOT cover the broken integration above.

- PR target confirmed: amule-org/amule (upstream). Source: origin = 3togo/amule, branch search-feature.
- Branch already committed (HEAD 63a283041). When approved: push origin search-feature then `gh pr create` against amule-org/amule master.

## search-feature rebase onto upstream/master (later in day)
- Fetched upstream; upstream/master tip now `25d97ee24` (local master was 1874 commits behind). search-feature branched from old master@2025-05-25.
- 22 files overlapped; squash-merge of search-feature onto upstream/master produced 120 conflict hunks across 18 files.
- Resolution strategy: `experimental/search-feature` tag preserved pre-rebase state. Did `git reset --hard upstream/master` on search-feature then `git merge --squash experimental/search-feature`.
  - `LibSocketAsio.cpp` (31 hunks): took upstream (ours) — dropped local Asio modernization since it's a separate PR (`fix/boost-asio-modernization`, already pushed to origin).
  - Feature C++ (SearchDlg/SearchFile/SearchList/SearchListCtrl + Kad Search/SearchManager/Indexed/KademliaUDPListener): kept feature version (theirs) wholesale.
  - `MD4Hash.h`: kept upstream single `Clear()` + added feature's `IsCorrupted()` (removed the duplicate `Clear()` the theirs side carried).
  - `.gitignore`: combined both sides. `ec/abstracts/CMakeLists.txt`: took upstream `cmake_minimum_required (VERSION 3.10...3.31)`. `FileLock.h`: kept feature's added `IsOk()`.
- Committed squash as `63a283041` "feat(search): rebase unified-search onto upstream/master". search-feature now 1 commit ahead of upstream/master; scope 101 files +24226/-7466. Build + runtime verification STILL PENDING (next step).

## Build verification of the rebased branch (later same day)
- Reconfigured cmake (clean) and built `amule` target. Fixes applied during reconciliation:
  - `MD4Hash.h`: re-added the missing `Clear()` method our conflict edit had dropped (it cascaded into ~70 errors).
  - Kademlia API drift (upstream changed signatures): `Indexed.cpp` `AddTag` now takes `(CTag*, uint32_t dbgSourceIP)` → pass `0`; `CKeyEntry::ReadPublishTrackingDataFromFile` now `(CFileDataIO*, bool includesAICH)` → pass `version >= 4`; `CSearchManager::ProcessResult` gained `(uint32_t fromIP, uint16_t fromPort)` → manager def updated, drops them (our CSearch::ProcessResult ignores contact IP); `KademliaUDPListener::ProcessSearchResponse` caller passes `0,0`.
- After those, build still FAILS with **54 errors** exposing a STRATEGIC blocker: the feature is a FORK of the search subsystem. Our `CSearchList`/`CSearchDlg` (UnifiedSearchEngineManager-based rewrite) implement NONE of upstream's caller API that the rest of aMule expects (`IsKnownSearchId`, `RegisterBrowseSearch`, `AllocateEd2kId`, `GetSearchFileByID`, `StopSearchById`, `BrowseSearch`, `SearchLifecycleState`, `SEARCH_LIFECYCLE_*`, `GetSearchLifecycleState/Percent/Kind(ById)`, `GetBrowsePeerEcid`, `AddFileToDownloadByEcid`, `GetCurrentSearchResultCount`, `ActivateBrowseTabIfOpen`, `SetBrowseStatus`, `OnSearchAdded`, `EnsureBrowseTab`, `CloseSearchTab`, `RefreshVisibleTabProgress`, `ApplySearchHistoryPref`, ...). The 6 caller files we did NOT rewrite (ExternalConn.cpp, GuiEvents.cpp, KnownFile.cpp, BaseClient.cpp, FriendListCtrl.cpp, PrefsUnifiedDlg.cpp) are now upstream's versions calling that API → break. Also `SearchListCtrl.h` includes `MuleListCtrl.h`, which upstream deleted.
- Conclusion: this cannot be trivially made build-green; it needs real reconciliation (add ~30 upstream methods to our class delegating to our engine, or rewrite 6 callers) — a large, architecturally-risky effort. Decision PENDING from user.

## Option A chosen + IMPLEMENTED (search-API reconciliation)
- User chose **A**: add upstream's `CSearchList`/`CSearchDlg` caller API to our fork's classes, backed by EXISTING state — NOT the orphaned `UnifiedSearchEngineManager` (which is NOT wired into `CSearchList`; the class is the classic aMule list + our `PerSearchState`/`m_results`/`SearchIdGenerator`/`SearchResultRouter` extensions).
- Implemented in `src/SearchList.h`/`.cpp` and `src/SearchDlg.h`/`.cpp`:
  - `SearchLifecycleState` enum nested in `CSearchList` (callers use `CSearchList::SEARCH_LIFECYCLE_*`); added `BrowseSearch=4` to `SearchType`.
  - Methods added: `StopSearchById`, `StopInFlightEd2kSearch`, `AllocateEd2kId`, `ReserveEd2kId`, `IsKadSearch`/`IsOrWasKadSearch`, `GetSearchStringById`, `IsKnownSearchId`, `GetKnownSearchIds`, `RequestMoreResults(uint32)`, `GetSearchProgress()` (no-arg), lifecycle (`GetSearchLifecycleState(ById)`, `GetSearchLifecyclePercent(ById)`, `GetSearchBarStatusById`, `GetSearchLifecycleKind(ById)`), `RegisterBrowseSearch`, `GetBrowsePeerEcid`, `GetCurrentSearchResultCount`, `StoreSearches`/`LoadSearches` (no-op), `GetSearchFileByID`, `GetAllSearchFilesByID`, `AddFileToDownloadByEcid`, `SetKadSearchFinished(uint32)`.
  - Lifecycle derived from `m_searchStates`/`m_results`/`m_browsePeers` + new bookkeeping maps (`m_searchStrings`,`m_searchKinds`,`m_searchStartTimes`,`m_browsePeers`) maintained in `StartNewSearch`/`RemoveResults`/`RegisterBrowseSearch`/`SetKadSearchFinished`. Kad uses a cosmetic time-ramp (60s).
  - Dialog methods: `OnSearchAdded`,`CloseSearchTab`,`EnsureBrowseTab`,`ActivateBrowseTabIfOpen`(false),`SetBrowseStatus`(noop),`UpdateSearchProgress`,`RefreshVisibleTabProgress`(noop),`ApplySearchHistoryPref`(noop),`MoreAllowed`/`MarkMoreExhausted`,`GetBrowseList`(nullptr),`AllocateOptimisticId`,`HasRunningEd2kSearch`(false),`GetVisibleSearchId`,`OnStartRejected`.
- RESULT: the 54 search-API errors in the 6 caller files are now RESOLVED (ExternalConn/Api/BaseClient/GuiEvents/KnownFile/FriendListCtrl/PrefsUnifiedDlg/amule-remote-gui compile).
- REMAINING 2 rebase-drift clusters (orthogonal to A, both from upstream modernization):
  1. **Kademlia API drift**: `CEntry::AddTag` now `(CTag*, uint32_t dbgSourceIP)` — Search.cpp + KademliaUDPListener.cpp still call 1-arg (just add `,0`); `ProcessSearchResponse*(...)` signatures changed in KademliaUDPListener.h vs .cpp definitions (reconcile .h/.cpp + callers); `GetTickCount`→`GetTickCount64`. Bounded, mechanical-ish.
  2. **ListCtrl framework migration**: upstream DELETED `MuleListCtrl.h`/`CMuleListCtrl` (wxListCtrl) → replaced with `CMuleDataViewCtrl` (wxDataViewCtrl). `SearchListCtrl.h` still `#include "MuleListCtrl.h"` (fatal) and the whole `SearchListCtrl.cpp` uses the old wxListCtrl API → needs full rewrite against DataView. LARGE, risky; needs separate decision.

## Remaining clusters ALSO RESOLVED (full build now green)
- **CMD4Hash `std::hash` regression (rebase)**: the `std::hash<CMD4Hash>` specialization lived in `MD4Hash.h` upstream, but the conflict resolution kept upstream's single `Clear()` + feature's `IsCorrupted()` and DROPPED the specialization → `ClientsWnd.cpp`/`ClientHistoryListCtrl.cpp`/`KademliaUDPListener.cpp` failed (`hash function must be copy constructible`). Fix: re-added `namespace std { template <> struct hash<CMD4Hash>{...} }` to `src/MD4Hash.h` (+ `#include <functional>`). Uses `RawPeekUInt64(GetHash())` mixing two words.
- **Kademlia API drift** (all fixed in `src/kademlia/net/KademliaUDPListener.cpp` + `src/kademlia/kademlia/Search.cpp`):
  - `CEntry::AddTag` is now 2-arg `(CTag*, uint32_t dbgSourceIP)`; added `, 0` to every 1-arg call site (incl. the `new CTagVarInt(...)` ones and the `CKeyEntry` inheritance case).
  - `ProcessSearchResponse(CMemFile&, uint32_t fromIP, uint16_t fromPort)`, `ProcessSearchResponse(packetData, lenPacket, fromIP, fromPort)`, `Process2SearchResponse(packetData, lenPacket, ip, port, senderKey)`, `ProcessSearchNotesResponse(packetData, lenPacket, ip, port)` — reconciled .cpp defs + the caller switch in `ProcessPacket` to pass `ip`/`port` down into `CSearchManager::ProcessResult(..., fromIP, fromPort)`.
  - `::GetTickCount()` → `::GetTickCount64()`.
- **ListCtrl framework migration — done via adoption, NOT a rewrite**: discovered our tree ALREADY had a DataView-based `src/SearchListModel.cpp`/`.h` (identical to upstream), but `SearchListCtrl.h`/`.cpp` had been left as the OLD wxListCtrl version by the rebase. Fix: overwrote `src/SearchListCtrl.h`/`.cpp` with upstream's DataView-based `CSearchListCtrl` (which pairs with the existing `SearchListModel`), then reconciled 2 differences:
  - `CSearchDlg` constructed `new CSearchListCtrl(..., wxLC_REPORT | wxNO_BORDER)` but upstream's ctor has no `style` param (5th is `name`); dropped the style arg in `SearchDlg.cpp`.
  - Added `SetSearchType(const wxString&)`/`GetSearchType()` + `m_searchType` to the adopted `CSearchListCtrl` (our dialog uses them; upstream didn't).
- **`SearchDlg.cpp` stale `muuli_wdr.h` symbols** (previously masked by the fatal include) fixed: `s_searchsizer`→`s_search_sizer`, `s_filtersizer`→`s_filter_sizer`, `s_extendedsizer`→`s_extended_sizer`, `IDC_SPINSEARCHAVAIBILITY`→`IDC_SPINSEARCHAVAILABILITY`, `ID_FILTER`→`ID_FILTER_RESET` (filter reset button in `EVT_BUTTON`), `DeleteAllItems()`→`ShowResults(0)`, `GetTickCount`→`GetTickCount64`.
- **OUTCOME**: `cmake --build build` green for ALL targets (0 errors); `ctest` 76/76 passed (incl. SearchTimeoutManagerTest etc.). The search-feature branch now BUILDS AND TESTS GREEN. No further blockers known.
