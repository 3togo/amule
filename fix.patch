diff --git a/src/amuleDlg.cpp b/src/amuleDlg.cpp
index 449d16311..newhash 100644
--- a/src/amuleDlg.cpp
+++ b/src/amuleDlg.cpp
@@ -1143,7 +1143,10 @@ void CamuleDlg::OnGUITimer(wxTimerEvent& WXUNUSED(evt))
                // trying to get the graph shifts evenly spaced after a change in the update period
                msPrevGraph = msCur;
 
-               GraphUpdateInfo update = theApp->m_statistics->GetPointsForUpdate();
+               // Check if statistics object is available before using it
+               if (theApp->m_statistics != NULL) {
+                       GraphUpdateInfo update = theApp->m_statistics->GetPointsForUpdate();
+
 
                m_statisticswnd->UpdateStatGraphs(theStats::GetPeakConnections(), update);
                m_kademliawnd->UpdateGraph(update);
@@ -1148,6 +1151,7 @@ void CamuleDlg::OnGUITimer(wxTimerEvent& WXUNUSED(evt))
                m_statisticswnd->UpdateStatGraphs(theStats::GetPeakConnections(), update);
                m_kademliawnd->UpdateGraph(update);
+               }
 
        int sStatsUpdate = thePrefs::GetStatsInterval();
        if ((sStatsUpdate > 0) && ((int)(msCur - msPrevStats) > sStatsUpdate*1000)) {
